#include <iostream>

int main(){


    /*

    order of operation:

    parentesis
    multiplication or division
    addition or subtraction

    */

    int students = 20;


    // Addition

    // students = students + 1;
    // students += 1;
    // students++;        //(only for increment of 1)

    // Subtraction
    
    // students = students - 1;
    // students -= 1;
    // students--;        //(only for a decrement of 1)

    // Multiplication
    
    // students = students * 2;
    // students *= 2;

    // Division
    
    // students = students / 2;
    // students /= 2;

    // Modulus - finds the remainder
    int remainder = students % 3;

    // std::cout<< students;
    std::cout<< remainder;

    return 0;
    sd w
}